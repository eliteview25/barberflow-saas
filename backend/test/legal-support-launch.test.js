const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'../..');
const auth=fs.readFileSync(path.join(root,'backend/src/routes/auth.js'),'utf8');const support=fs.readFileSync(path.join(root,'backend/src/routes/support.js'),'utf8');const app=fs.readFileSync(path.join(root,'backend/src/app.js'),'utf8');const common=fs.readFileSync(path.join(root,'frontend/js/common.js'),'utf8');
test('cadastro exige e registra aceite de termos e privacidade',()=>{assert.match(auth,/aceite_termos !== true/);assert.match(auth,/aceite_privacidade !== true/);assert.match(auth,/INSERT INTO legal_acceptances/);assert.match(auth,/'termos'/);assert.match(auth,/'privacidade'/)});
test('páginas legais essenciais existem',()=>{for(const f of ['termos.html','privacidade.html','lgpd.html','cancelamento.html'])assert.ok(fs.existsSync(path.join(root,'frontend',f)),f)});
test('suporte permanece disponível sem gate de assinatura e é isolado por tenant',()=>{assert.match(support,/router\.use\(autenticar\)/);assert.doesNotMatch(support,/exigirAssinatura/);assert.match(support,/WHERE barbearia_id=\$1/);assert.match(support,/req\.usuario\.barbearia_id/);assert.match(app,/app\.use\('\/api\/suporte',support\)/);assert.match(common,/\/pages\/suporte\.html/)});
test('cliente pode acompanhar resposta sem expor credenciais',()=>{const page=fs.readFileSync(path.join(root,'frontend/pages/suporte.html'),'utf8');assert.match(page,/Nunca envie senhas, códigos 2FA ou dados completos de cartão/);assert.match(fs.readFileSync(path.join(root,'frontend/js/suporte.js'),'utf8'),/Resposta do EliteFlow/)});
