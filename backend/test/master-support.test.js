const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'../..');const route=fs.readFileSync(path.join(root,'backend/src/routes/master.js'),'utf8');const html=fs.readFileSync(path.join(root,'frontend/master.html'),'utf8');const js=fs.readFileSync(path.join(root,'frontend/js/master.js'),'utf8');
test('Supermaster recebe central de suporte e resposta exige step-up',()=>{assert.match(route,/router\.get\('\/support\/tickets'/);assert.match(route,/router\.patch\('\/support\/tickets\/:id',exigirStepUp/);assert.match(html,/id="suporte-sec"/);assert.match(js,/async function suporte\(\)/)});
