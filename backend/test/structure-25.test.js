const test=require('node:test'),assert=require('node:assert/strict'),fs=require('fs'),path=require('path');const root=path.join(__dirname,'../..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('menu lateral prioriza operação de barbearia',()=>{const c=read('frontend/js/common.js');for(const k of ['agendamentos','clientes','barbeiros','servicos','pagina-publica','gestao-pdv','gestao-vendas','gestao-produtos','gestao-comissoes','automacoes'])assert.ok(c.includes(`'${k}'`));assert.match(c,/groupHtml\('operacao'[\s\S]*gestao-pdv[\s\S]*gestao-vendas/);assert.match(c,/Produtos & Estoque/);assert.match(c,/WhatsApp & Automações/);assert.doesNotMatch(c,/lojaGroupHtml/)});
