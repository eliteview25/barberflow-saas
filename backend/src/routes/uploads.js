const express=require('express');const multer=require('multer');const crypto=require('crypto');const {autenticar,exigirPapel}=require('../middlewares/auth');const {exigirRecurso}=require('../services/planos');const router=express.Router();
const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:5*1024*1024},fileFilter:(req,file,cb)=>cb(null,/^image\/(png|jpe?g|webp|gif)$/.test(file.mimetype))});
router.use(autenticar,exigirPapel('dono','gerente'),exigirRecurso('personalizacao_publica'));
router.post('/imagem',upload.single('imagem'),async(req,res)=>{try{if(!req.file)return res.status(400).json({erro:'Selecione uma imagem PNG, JPG, WEBP ou GIF de até 5MB'});const cloud=process.env.CLOUDINARY_CLOUD_NAME,key=process.env.CLOUDINARY_API_KEY,secret=process.env.CLOUDINARY_API_SECRET;if(!cloud||!key||!secret)return res.status(503).json({erro:'Upload ainda não configurado. Configure Cloudinary no Render.'});const timestamp=Math.floor(Date.now()/1000),folder=`barberflow/${req.usuario.barbearia_id}`;const signature=crypto.createHash('sha1').update(`folder=${folder}&timestamp=${timestamp}${secret}`).digest('hex');const fd=new FormData();fd.append('file',new Blob([req.file.buffer],{type:req.file.mimetype}),req.file.originalname);fd.append('api_key',key);fd.append('timestamp',String(timestamp));fd.append('folder',folder);fd.append('signature',signature);const r=await fetch(`https://api.cloudinary.com/v1_1/${cloud}/image/upload`,{method:'POST',body:fd});const d=await r.json();if(!r.ok)throw new Error(d?.error?.message||'Falha no upload');res.status(201).json({url:d.secure_url,public_id:d.public_id,width:d.width,height:d.height})}catch(e){console.error(e);res.status(500).json({erro:e.message||'Erro ao enviar imagem'})}});
module.exports=router;
